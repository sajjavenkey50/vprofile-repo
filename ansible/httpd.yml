---
- name: setup web server
  hosts: aws_ec2
  become: yes
  tasks:
    - name: installing apache httpd service
      apt:
        name: apache2
        state: present
        update_cache: yes
    - name: start and enable httpd service
      service:
        name: apache2
        state: started
        enabled: yes
        
